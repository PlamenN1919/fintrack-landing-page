const passiveSupported = (() => { let passive = false; try { const options = { get passive() { passive = true; return false; } }; window.addEventListener('test', null, options); window.removeEventListener('test', null, options); } catch (err) { passive = false; } return passive;})();const passiveEvent = passiveSupported ? { passive: true } : false;function debounce(func, wait) { let timeout; return function executedFunction(...args) { const later = () => { clearTimeout(timeout); func(...args); }; clearTimeout(timeout); timeout = setTimeout(later, wait); };}function throttle(func, limit) { let inThrottle; return function(...args) { if (!inThrottle) { func.apply(this, args); inThrottle = true; setTimeout(() => inThrottle = false, limit); } };}let locomotiveScroll = null;function removeSectionBorders() {  const sections = document.querySelectorAll('[data-scroll-section], .section, section'); sections.forEach(section => { section.style.setProperty('border', 'none', 'important'); section.style.setProperty('outline', 'none', 'important'); section.style.setProperty('box-shadow', 'none', 'important'); section.style.setProperty('border-top', '0', 'important'); section.style.setProperty('border-bottom', '0', 'important'); section.style.setProperty('border-left', '0', 'important'); section.style.setProperty('border-right', '0', 'important'); section.style.setProperty('border-width', '0', 'important'); section.style.setProperty('border-style', 'none', 'important'); });   const container = document.querySelector('[data-scroll-container]'); if (container) { container.style.setProperty('border', 'none', 'important'); container.style.setProperty('outline', 'none', 'important'); container.style.setProperty('box-shadow', 'none', 'important'); }}function initLocomotiveScroll() { const isMobile = window.innerWidth <= 768;  locomotiveScroll = new LocomotiveScroll({ el: document.querySelector('[data-scroll-container]'), smooth: !isMobile,  lerp: 0.15,  multiplier: 1.0,  smartphone: { smooth: false  }, tablet: { smooth: false  }, reloadOnContextChange: true, touchMultiplier: 2.5, smoothMobile: false });   setTimeout(() => { removeSectionBorders(); }, 100);   let resizeTimeout; let wasMobile = isMobile;  window.addEventListener('resize', () => { clearTimeout(resizeTimeout); resizeTimeout = setTimeout(() => { const nowMobile = window.innerWidth <= 768;   if (wasMobile !== nowMobile && locomotiveScroll) { locomotiveScroll.destroy(); locomotiveScroll = initLocomotiveScroll(); console.log('🔄 Locomotive reloaded for device change'); } else if (locomotiveScroll) { locomotiveScroll.update(); }  wasMobile = nowMobile; }, 250); });   const images = document.querySelectorAll('img'); images.forEach(img => { if (img.complete) { if (locomotiveScroll) locomotiveScroll.update(); } else { img.addEventListener('load', () => { if (locomotiveScroll) locomotiveScroll.update(); }); } });  return locomotiveScroll;}const nav = document.querySelector('.main-nav');const heroTitle = document.querySelector('.brand-title');const sections = document.querySelectorAll('.section');const featureCards = document.querySelectorAll('.feature-card');const scrollIndicator = document.querySelector('.scroll-indicator');const splineContainer = document.querySelector('.spline-container');const splineViewer = document.querySelector('spline-viewer');let lossCounter = 0;let counterInterval;function initSeamlessReveal() {  const useLocomotiveCallbacks = locomotiveScroll && window.innerWidth > 768;  if (useLocomotiveCallbacks) {  locomotiveScroll.on('call', (func, way, obj) => {  if (func === 'revealSection') { obj.el.classList.add('section-visible'); } });   setTimeout(() => { if (locomotiveScroll) locomotiveScroll.update(); }, 500); }   const sectionObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('section-visible');  if (locomotiveScroll && window.innerWidth > 768) { locomotiveScroll.update(); } } }); }, { threshold: 0.15, rootMargin: '0px 0px -50px 0px' });  const revealObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('active'); if (locomotiveScroll && window.innerWidth > 768) { locomotiveScroll.update(); } } }); }, { threshold: 0.1, rootMargin: '0px 0px -80px 0px' });  const staggerObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('active'); if (locomotiveScroll && window.innerWidth > 768) { locomotiveScroll.update(); } } }); }, { threshold: 0.2, rootMargin: '0px 0px -50px 0px' });  document.querySelectorAll('.section').forEach(section => { sectionObserver.observe(section); });  const footer = document.querySelector('.footer'); if (footer) { sectionObserver.observe(footer); }  document.querySelectorAll('.reveal, .reveal-left, .reveal-right, .reveal-scale, .reveal-rotate, .reveal-fade').forEach(el => { revealObserver.observe(el); });  document.querySelectorAll('.stagger-children').forEach(el => { staggerObserver.observe(el); });}function initParallaxEffects() { const isMobile = window.innerWidth <= 768;    if (isMobile || !locomotiveScroll) {  const throttledParallax = throttle(() => { window.requestAnimationFrame(() => { updateParallax(window.pageYOffset); }); }, 16);   window.addEventListener('scroll', throttledParallax, passiveEvent); } else {  locomotiveScroll.on('scroll', (args) => { updateParallax(args.scroll.y); }); }}function updateParallax(scrolled) { const isMobile = window.innerWidth <= 768; const speedMultiplier = isMobile ? 0.5 : 0.7;    document.querySelectorAll('.fear-geometric-shape').forEach((shape, index) => { const speed = (index + 1) * 0.015 * speedMultiplier;  const yPos = scrolled * speed; shape.style.transform = `translate3d(0, ${yPos}px, 0)`;  });  document.querySelectorAll('.stats-shape').forEach((shape, index) => { const speed = (index + 1) * 0.01 * speedMultiplier;  const yPos = scrolled * speed; shape.style.transform = `translate3d(0, ${yPos}px, 0)`; });  document.querySelectorAll('.how-shape').forEach((shape, index) => { const speed = (index + 1) * 0.012 * speedMultiplier;  const yPos = scrolled * speed; shape.style.transform = `translate3d(0, ${yPos}px, 0)`; });   document.querySelectorAll('.parallax-slow').forEach(el => { const rect = el.getBoundingClientRect(); const elementTop = rect.top + scrolled; const distanceFromTop = scrolled - elementTop + window.innerHeight; const yPos = distanceFromTop * 0.025 * speedMultiplier;  el.style.transform = `translate3d(0, ${yPos}px, 0)`; });  document.querySelectorAll('.parallax-medium').forEach(el => { const rect = el.getBoundingClientRect(); const elementTop = rect.top + scrolled; const distanceFromTop = scrolled - elementTop + window.innerHeight; const yPos = distanceFromTop * 0.1 * speedMultiplier; el.style.transform = `translateY(${yPos}px)`; });  document.querySelectorAll('.parallax-fast').forEach(el => { const rect = el.getBoundingClientRect(); const elementTop = rect.top + scrolled; const distanceFromTop = scrolled - elementTop + window.innerHeight; const yPos = distanceFromTop * 0.15 * speedMultiplier; el.style.transform = `translateY(${yPos}px)`; });}function initSmoothMomentum() {  let isScrolling = false; let scrollTimeout;  const handleScrollState = () => { if (!isScrolling) { document.body.classList.add('is-scrolling'); isScrolling = true; }  clearTimeout(scrollTimeout); scrollTimeout = setTimeout(() => { document.body.classList.remove('is-scrolling'); isScrolling = false; }, 150); };  window.addEventListener('scroll', handleScrollState, passiveEvent);   if (locomotiveScroll) { locomotiveScroll.on('scroll', handleScrollState); }}window.addEventListener('beforeunload', () => { if (locomotiveScroll) { locomotiveScroll.destroy(); console.log('🧹 Locomotive cleaned up'); }});function initFeaturesAnimations() { const featureCards = document.querySelectorAll('.feature-card-animated'); const isMobile = window.innerWidth <= 768;   if (isMobile) { console.log('✨ Features animations initialized (mobile mode)!'); return; }   featureCards.forEach(card => { let isHovering = false;  card.addEventListener('mouseenter', () => { isHovering = true; card.style.transition = 'none'; });  card.addEventListener('mousemove', (e) => { if (!isHovering) return;  const rect = card.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top;  const centerX = rect.width / 2; const centerY = rect.height / 2;  const rotateX = (y - centerY) / 20; const rotateY = (centerX - x) / 20;  card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateY(-8px) scale(1.02)`;   const glow = card.querySelector('.card-glow'); if (glow) { const glowX = (x / rect.width) * 100; const glowY = (y / rect.height) * 100; glow.style.background = `radial-gradient(circle at ${glowX}% ${glowY}%, rgba(255,255,255,0.15), transparent 60%)`; } });  card.addEventListener('mouseleave', () => { isHovering = false; card.style.transition = ''; card.style.transform = '';  const glow = card.querySelector('.card-glow'); if (glow) { glow.style.background = ''; } });   card.addEventListener('click', (e) => { const ripple = document.createElement('div'); ripple.className = 'card-ripple';  const rect = card.getBoundingClientRect(); const size = Math.max(rect.width, rect.height) * 2; const x = e.clientX - rect.left - size / 2; const y = e.clientY - rect.top - size / 2;  ripple.style.width = ripple.style.height = size + 'px'; ripple.style.left = x + 'px'; ripple.style.top = y + 'px';  card.appendChild(ripple);  setTimeout(() => ripple.remove(), 600); });   card.addEventListener('mouseenter', () => { createSparkles(card); }); });   if (locomotiveScroll) { locomotiveScroll.on('scroll', (args) => { featureCards.forEach(card => { if (card.classList.contains('card-revealed')) { const icon = card.querySelector('.card-icon i'); if (icon) { const rect = card.getBoundingClientRect(); const scrollProgress = (window.innerHeight - rect.top) / window.innerHeight; if (scrollProgress > 0 && scrollProgress < 1) { icon.style.transform = `scale(${1 + scrollProgress * 0.1})`; } } } }); }); }  console.log('✨ Features animations initialized!');}function createSparkles(card) { const sparkleCount = 3;  for (let i = 0; i < sparkleCount; i++) { setTimeout(() => { const sparkle = document.createElement('div'); sparkle.className = 'sparkle'; sparkle.innerHTML = '✨';  const x = Math.random() * 100; const y = Math.random() * 100;  sparkle.style.position = 'absolute'; sparkle.style.left = x + '%'; sparkle.style.top = y + '%'; sparkle.style.pointerEvents = 'none'; sparkle.style.fontSize = '12px'; sparkle.style.opacity = '0'; sparkle.style.animation = 'sparkleFloat 1.5s ease-out forwards'; sparkle.style.zIndex = '20';  card.appendChild(sparkle);  setTimeout(() => sparkle.remove(), 1500); }, i * 100); }}function animateCardNumbers() { const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting && !entry.target.classList.contains('numbers-animated')) { entry.target.classList.add('numbers-animated');   const numbers = entry.target.querySelectorAll('[data-number]'); numbers.forEach(num => { const target = parseInt(num.getAttribute('data-number')); animateNumber(num, 0, target, 1500); }); } }); }, { threshold: 0.3 });  document.querySelectorAll('.feature-card-animated').forEach(card => { observer.observe(card); });}function animateNumber(element, start, end, duration) { const startTime = performance.now();  function update(currentTime) { const elapsed = currentTime - startTime; const progress = Math.min(elapsed / duration, 1);   const easeOutQuart = 1 - Math.pow(1 - progress, 4); const current = Math.floor(start + (end - start) * easeOutQuart);  element.textContent = current;  if (progress < 1) { requestAnimationFrame(update); } else { element.textContent = end; } }  requestAnimationFrame(update);}setTimeout(() => { animateCardNumbers();}, 500);document.addEventListener('DOMContentLoaded', () => {  locomotiveScroll = initLocomotiveScroll(); console.log('🚂 Locomotive Scroll initialized!');   setTimeout(() => { initSeamlessReveal(); initParallaxEffects(); initSmoothMomentum(); initFeaturesAnimations(); console.log('Seamless entrance animations initialized! ✨');   if (locomotiveScroll) { locomotiveScroll.update(); console.log('📏 Locomotive heights updated'); } }, 100);   setTimeout(() => { if (locomotiveScroll) { locomotiveScroll.update(); console.log('📏 Final Locomotive update'); } }, 500);   window.addEventListener('load', () => { if (locomotiveScroll) { locomotiveScroll.update(); console.log('📏 Locomotive updated on window load'); } });});function handleScrollEffects(scrollTop) {  if (nav && scrollTop > 100) { nav.style.background = 'rgba(26, 26, 26, 0.3)'; } else if (nav) { nav.style.background = 'rgba(26, 26, 26, 0.1)'; }   if (scrollIndicator) { if (scrollTop > 200) { scrollIndicator.style.opacity = '0'; } else { scrollIndicator.style.opacity = '1'; } }}if (locomotiveScroll) { locomotiveScroll.on('scroll', (args) => { handleScrollEffects(args.scroll.y); });}const throttledScrollEffects = throttle(() => { handleScrollEffects(window.pageYOffset);}, 16); window.addEventListener('scroll', throttledScrollEffects, passiveEvent);document.querySelectorAll('a[href^="#"]').forEach(anchor => { anchor.addEventListener('click', function (e) { e.preventDefault(); const targetId = this.getAttribute('href'); const target = document.querySelector(targetId);  if (target) { if (locomotiveScroll && window.innerWidth > 768) {  locomotiveScroll.scrollTo(target, { offset: 0, duration: 1000, easing: [0.25, 0.0, 0.35, 1.0] }); } else {  target.scrollIntoView({ behavior: 'smooth', block: 'start' }); } } });});const observerOptions = { threshold: 0.1, rootMargin: '0px 0px -50px 0px'};const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('animate-in'); } });}, observerOptions);sections.forEach(section => { observer.observe(section);});featureCards.forEach((card, index) => { card.style.animationDelay = `${index * 0.2}s`; observer.observe(card);});const phoneMockup = document.querySelector('.phone-mockup');if (phoneMockup) { const phoneObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { phoneMockup.style.animation = 'slideInRight 1s ease forwards'; } }); }, { threshold: 0.3 });  phoneObserver.observe(phoneMockup);}const stats = document.querySelectorAll('.stat-number');const statsObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { const stat = entry.target; const finalValue = stat.textContent; animateCounter(stat, finalValue); } });}, { threshold: 0.5 });stats.forEach(stat => { statsObserver.observe(stat);});function animateCounter(element, finalValue) { const isNumberOnly = /^\d+/.test(finalValue); if (!isNumberOnly) return;  const numericValue = parseInt(finalValue.replace(/\D/g, '')); const suffix = finalValue.replace(/[\d,]/g, ''); const duration = 1500;  const steps = 40;  const increment = numericValue / steps;  let current = 0; const timer = setInterval(() => { current += increment; if (current >= numericValue) { element.textContent = finalValue; clearInterval(timer); } else { const displayValue = Math.floor(current); element.textContent = displayValue.toLocaleString() + suffix; } }, duration / steps);}function showNotification(message, type = 'info') {  const existingNotifications = document.querySelectorAll('.notification'); existingNotifications.forEach(notification => notification.remove());   const notification = document.createElement('div'); notification.className = `notification notification-${type}`; notification.innerHTML = ` <span>${message}</span> <button class="notification-close">&times;</button> `;   Object.assign(notification.style, { position: 'fixed', top: '20px', right: '20px', padding: '15px 20px', borderRadius: '10px', color: '#ffffff', fontSize: '14px', fontWeight: '500', zIndex: '10000', transform: 'translateX(100%)', transition: 'transform 0.3s ease', maxWidth: '400px', boxShadow: '0 10px 30px rgba(0,0,0,0.3)' });   switch (type) { case 'success': notification.style.background = '#4CAF50'; break; case 'error': notification.style.background = '#f44336'; break; default: notification.style.background = '#2196F3'; }   document.body.appendChild(notification);   setTimeout(() => { notification.style.transform = 'translateX(0)'; }, 100);   const closeBtn = notification.querySelector('.notification-close'); closeBtn.style.cssText = ` background: none; border: none; color: #ffffff; font-size: 18px; margin-left: 10px; cursor: pointer; opacity: 0.7; `;  closeBtn.addEventListener('click', () => { notification.style.transform = 'translateX(100%)'; setTimeout(() => notification.remove(), 300); });  closeBtn.addEventListener('mouseenter', () => { closeBtn.style.opacity = '1'; });  closeBtn.addEventListener('mouseleave', () => { closeBtn.style.opacity = '0.7'; });   setTimeout(() => { if (notification.parentNode) { notification.style.transform = 'translateX(100%)'; setTimeout(() => notification.remove(), 300); } }, 5000);}const heroParallax = throttle(() => { const scrolled = window.pageYOffset; const rate = scrolled * -0.5;  if (heroTitle) { heroTitle.style.transform = `translate3d(0, ${rate}px, 0)`;  }}, 16); window.addEventListener('scroll', heroParallax, passiveEvent);featureCards.forEach(card => { card.addEventListener('mouseenter', function() { this.style.transform = 'translateY(-15px) scale(1.02)'; });  card.addEventListener('mouseleave', function() { this.style.transform = 'translateY(0) scale(1)'; });});document.querySelectorAll('.download-btn').forEach(btn => { btn.addEventListener('click', function(e) { e.preventDefault(); const platform = this.classList.contains('ios') ? 'iOS' : 'Android'; showNotification(`Пренасочване към ${platform} магазина...`, 'info');   this.style.transform = 'translateY(-8px) scale(0.98)'; setTimeout(() => { this.style.transform = 'translateY(-5px) scale(1)'; }, 150);   setTimeout(() => {  const storeUrl = platform === 'iOS' ?  'https: 'https:  }, 1000); });});const style = document.createElement('style');style.textContent = ` @keyframes slideInRight { from { transform: translateX(50px); } to { transform: translateX(0); } }  @keyframes slideInLeft { from { transform: translateX(-50px); } to { transform: translateX(0); } }  @keyframes slideInUp { from { transform: translateY(50px); } to { transform: translateY(0); } }  .feature-card { transform: translateY(30px); transition: transform 0.6s ease; }  .feature-card.animate-in { transform: translateY(0); }`;document.head.appendChild(style);function debounce(func, wait) { let timeout; return function executedFunction(...args) { const later = () => { clearTimeout(timeout); func(...args); }; clearTimeout(timeout); timeout = setTimeout(later, wait); };}const debouncedScrollHandler = debounce(() => { const scrollTop = window.pageYOffset;   if (nav && scrollTop > 100) { nav.style.background = 'rgba(26, 26, 26, 0.95)'; } else if (nav) { nav.style.background = 'rgba(26, 26, 26, 0.9)'; }   if (scrollIndicator) { if (scrollTop > 200) { scrollIndicator.style.opacity = '0'; } else { scrollIndicator.style.opacity = '1'; } }   const rate = scrollTop * -0.5; if (heroTitle) { heroTitle.style.transform = `translateY(${rate}px)`; }}, 10);window.addEventListener('scroll', debouncedScrollHandler);document.addEventListener('DOMContentLoaded', () => {  const hero = document.querySelector('.hero'); if (hero) { hero.classList.add('animate-in'); }   const imageUrls = []; imageUrls.forEach(url => { const img = new Image(); img.src = url; });});document.addEventListener('keydown', (e) => {  if (e.key === 'Escape') { const notifications = document.querySelectorAll('.notification'); notifications.forEach(notification => { notification.style.transform = 'translateX(100%)'; setTimeout(() => notification.remove(), 300); }); }});document.querySelectorAll('a, button, input, textarea').forEach(element => { element.addEventListener('focus', function() { this.style.outline = '2px solid #4CAF50'; this.style.outlineOffset = '2px'; });  element.addEventListener('blur', function() { this.style.outline = 'none'; });});if (splineViewer && splineContainer) {  splineViewer.addEventListener('load', function() { setTimeout(() => { splineViewer.classList.add('loaded'); splineContainer.classList.add('loaded'); }, 100); });   setTimeout(() => { if (!splineViewer.classList.contains('loaded')) { splineViewer.classList.add('loaded'); splineContainer.classList.add('loaded'); } }, 2000);   setTimeout(() => { if (splineViewer) { splineViewer.style.opacity = '0.7'; } }, 500);}document.addEventListener('DOMContentLoaded', function() { console.log('FinTrack Landing Page загредена успешно! 🚀'); initPainPointCalculator(); initFearOfLossSection();});function initPainPointCalculator() { initLossCounter(); initCalculatorEvents(); initPainPointAnimations(); enhanceFloatingIcons();}function initLossCounter() { counterInterval = setInterval(() => { lossCounter += 0.33; const lossElement = document.getElementById('liveLossCounter'); if (lossElement) { lossElement.textContent = lossCounter.toFixed(2) + ' лв'; } }, 1000);}function initCalculatorEvents() {  const savingsSlider = document.getElementById('savingsRate'); if (savingsSlider) { savingsSlider.addEventListener('input', function() { const percentElement = document.getElementById('savingsPercent'); if (percentElement) { percentElement.textContent = this.value; } }); }   const calculateBtn = document.querySelector('.calculate-btn'); if (calculateBtn) { calculateBtn.addEventListener('click', () => {  if (typeof gtag !== 'undefined') { gtag('event', 'calculator_started', { 'event_category': 'engagement', 'event_label': 'pain_point_calculator' }); } }); }}function calculateLoss() { const income = parseFloat(document.getElementById('monthlyIncome').value) || 3000; const trackingValue = document.querySelector('input[name="tracking"]:checked')?.value || 'never'; const savingsRate = parseFloat(document.getElementById('savingsRate').value) || 5;   let lossMultiplier; switch(trackingValue) { case 'never': lossMultiplier = 0.35; break; case 'sometimes': lossMultiplier = 0.20; break; case 'always': lossMultiplier = 0.10; break; default: lossMultiplier = 0.35; }   const idealSavingsRate = Math.min(30, savingsRate + 15);  const currentSavings = income * (savingsRate / 100); const potentialSavings = income * (idealSavingsRate / 100); const monthlyLoss = (potentialSavings - currentSavings) + (income * lossMultiplier * 0.1);   const monthlyLossEl = document.getElementById('monthlyLoss'); const yearlyLossEl = document.getElementById('yearlyLoss'); const lifetimeLossEl = document.getElementById('lifetimeLoss');  if (monthlyLossEl) monthlyLossEl.textContent = Math.round(monthlyLoss) + ' лв'; if (yearlyLossEl) yearlyLossEl.textContent = Math.round(monthlyLoss * 12) + ' лв'; if (lifetimeLossEl) lifetimeLossEl.textContent = Math.round(monthlyLoss * 120) + ' лв';   const resultsContainer = document.getElementById('calculatorResults'); if (resultsContainer) { resultsContainer.style.display = 'block'; resultsContainer.scrollIntoView({ behavior: 'smooth', block: 'nearest' });   if (monthlyLoss > 500) { resultsContainer.style.animation = 'shockPulse 0.5s ease-in-out'; } }   if (typeof gtag !== 'undefined') { gtag('event', 'calculator_completed', { 'monthly_loss': Math.round(monthlyLoss), 'income_range': getIncomeRange(income), 'tracking_habit': trackingValue }); }}function getIncomeRange(income) { if (income < 1500) return 'low'; if (income < 3000) return 'medium'; if (income < 5000) return 'high'; return 'very_high';}function initPainPointAnimations() { const observerOptions = { threshold: 0.3, rootMargin: '0px' };  const painObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) {  const statNumbers = entry.target.querySelectorAll('.stat-number, .stat-num'); statNumbers.forEach((stat, index) => { setTimeout(() => { animateNumber(stat); }, index * 200); });   const warningBadge = entry.target.querySelector('.warning-badge'); if (warningBadge) { warningBadge.style.animation = 'urgentPulse 2s infinite'; } } }); }, observerOptions);  const painSection = document.querySelector('.pain-calculator-section'); if (painSection) { painObserver.observe(painSection); }}function animateNumber(element) { const originalText = element.textContent; const target = parseInt(originalText.replace(/[^\d]/g, '')); const suffix = originalText.replace(/[\d,]/g, ''); let current = 0; const increment = target / 60;   function updateNumber() { current += increment; if (current < target) { element.textContent = Math.floor(current).toLocaleString() + suffix; requestAnimationFrame(updateNumber); } else { element.textContent = target.toLocaleString() + suffix; } }  updateNumber();}function enhanceFloatingIcons() { const icons = document.querySelectorAll('.floating-icon'); icons.forEach((icon, index) => {  setInterval(() => { const randomX = (Math.random() - 0.5) * 20; const randomY = (Math.random() - 0.5) * 20; icon.style.transform = `translate(${randomX}px, ${randomY}px)`; }, 3000 + index * 500);   icon.addEventListener('mouseenter', () => { icon.style.transform = 'scale(1.2)'; icon.style.color = '#ffc107'; });  icon.addEventListener('mouseleave', () => { icon.style.transform = 'scale(1)'; icon.style.color = 'rgba(255, 255, 255, 0.6)'; }); });}const additionalStyles = `@keyframes urgentPulse { 0%, 100% {  transform: scale(1);  box-shadow: 0 0 0 0 rgba(255, 107, 107, 0.4); } 50% {  transform: scale(1.05);  box-shadow: 0 0 0 10px rgba(255, 107, 107, 0); }}@keyframes shockPulse { 0% { transform: scale(1); } 25% { transform: scale(1.02); } 50% { transform: scale(1); } 75% { transform: scale(1.02); } 100% { transform: scale(1); }}.floating-icon { transition: all 0.3s ease;}`;const styleSheet = document.createElement('style');styleSheet.textContent = additionalStyles;document.head.appendChild(styleSheet);function initFearOfLossSection() {  initFearAnimations(); initTimelineAnimations(); initEnhancedFearFeatures();}function initFearAnimations() { const observerOptions = { threshold: 0.2, rootMargin: '0px' };  const fearObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) {  const geometricShapes = entry.target.querySelectorAll('.fear-geometric-shape'); geometricShapes.forEach((shape, index) => { setTimeout(() => { shape.style.opacity = '0.15'; shape.style.animation = `floatGeometric 20s ease-in-out infinite ${index * -5}s`; }, index * 200); });   const glassCards = entry.target.querySelectorAll('.fear-glass-card'); glassCards.forEach((card, index) => { setTimeout(() => { card.style.opacity = '1'; card.style.transform = 'translateX(0) scale(1)';   setTimeout(() => { card.addEventListener('mouseenter', () => { card.style.transform = 'translateY(-8px) scale(1.02)'; card.style.boxShadow = '0 25px 50px rgba(0, 0, 0, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.15), 0 0 40px rgba(255, 107, 107, 0.3)'; card.style.background = 'linear-gradient(135deg, rgba(44, 44, 44, 0.95) 0%, rgba(44, 44, 44, 0.85) 50%, rgba(44, 44, 44, 0.95) 100%)'; });  card.addEventListener('mouseleave', () => { card.style.transform = 'translateY(0) scale(1)'; card.style.boxShadow = '0 12px 40px rgba(0, 0, 0, 0.4), 0 4px 16px rgba(0, 0, 0, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.1), inset 0 -1px 0 rgba(0, 0, 0, 0.2)'; card.style.background = 'linear-gradient(135deg, rgba(44, 44, 44, 0.85) 0%, rgba(44, 44, 44, 0.75) 50%, rgba(44, 44, 44, 0.85) 100%)'; }); }, 500); }, index * 300 + 500); });   const floatingWarning = entry.target.querySelector('.fear-floating-warning'); if (floatingWarning) { setTimeout(() => { floatingWarning.style.opacity = '1'; floatingWarning.style.transform = 'scale(1) rotate(0deg)'; }, 1800); }  } }); }, observerOptions);  const fearSection = document.querySelector('.fear-of-loss-section-new'); if (fearSection) { fearObserver.observe(fearSection);   const glassCards = fearSection.querySelectorAll('.fear-glass-card'); glassCards.forEach(card => {  card.addEventListener('mouseenter', () => { const cardIcon = card.querySelector('.fear-card-icon i'); if (cardIcon) { cardIcon.style.transform = 'rotate(5deg) scale(1.1)'; cardIcon.style.boxShadow = '0 15px 35px rgba(255, 107, 107, 0.6)'; }  });  card.addEventListener('mouseleave', () => { const cardIcon = card.querySelector('.fear-card-icon i'); if (cardIcon) { cardIcon.style.transform = 'rotate(0deg) scale(1)'; cardIcon.style.boxShadow = '0 10px 25px rgba(255, 107, 107, 0.4)'; }  }); });  }}function initTimelineAnimations() { const timelineItems = document.querySelectorAll('.timeline-item');  timelineItems.forEach((item, index) => { item.addEventListener('mouseenter', () => {  item.classList.add('active');   const dot = item.querySelector('.timeline-dot'); if (dot) { dot.style.transform = 'scale(1.5)'; dot.style.zIndex = '10'; }   if (item.classList.contains('critical') || item.classList.contains('devastating')) { item.style.animation = item.classList.contains('devastating') ?  'devastatingShake 1s ease-in-out infinite' :  'criticalPulse 1.5s ease-in-out infinite'; } });  item.addEventListener('mouseleave', () => { const dot = item.querySelector('.timeline-dot'); if (dot) { dot.style.transform = 'scale(1)'; dot.style.zIndex = '2'; }   if (!item.classList.contains('active')) { item.style.animation = ''; } });   setTimeout(() => { item.classList.add('revealed'); item.style.opacity = '1'; item.style.transform = 'translateY(0)'; }, index * 400 + 1000); });   timelineItems.forEach(item => { item.style.opacity = '0'; item.style.transform = 'translateY(20px)'; item.style.transition = 'opacity 0.5s ease, transform 0.5s ease'; });   const observerOptions = { threshold: 0.5, rootMargin: '0px' };  const graphObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { const bars = entry.target.querySelectorAll('.bar'); bars.forEach((bar, index) => { setTimeout(() => { bar.style.animation = `barGrow 1s ease-out ${index * 0.2}s both, barPulse 2s ease-in-out infinite ${index * 0.2 + 1}s`; }, 500); }); } }); }, observerOptions);  const lossGraph = document.querySelector('.loss-graph'); if (lossGraph) { graphObserver.observe(lossGraph); }}function stopLosses() {  if (typeof gtag !== 'undefined') { gtag('event', 'fear_salvation_attempt', { 'event_category': 'high_intent_conversion', 'event_label': 'fear_of_loss_section', 'emotional_state': 'fear_activated' }); }   const salvationBtn = document.querySelector('.salvation-btn'); if (salvationBtn) { const originalContent = salvationBtn.innerHTML; salvationBtn.innerHTML = ` <div style="display: flex; align-items: center; gap: 10px;"> <i class="fas fa-shield-alt fa-spin"></i> <span>АКТИВИРАНЕ НА ЗАЩИТАТА...</span> </div> `; salvationBtn.disabled = true; salvationBtn.style.background = 'linear-gradient(45deg, #28a745, #ffc107)';   const fearSection = document.querySelector('.fear-of-loss-section'); if (fearSection) { fearSection.style.animation = 'salvationActivated 1s ease-out'; }  setTimeout(() => { salvationBtn.innerHTML = originalContent; salvationBtn.disabled = false;   showSalvationSuccess(); }, 3000); }   clearInterval(window.fearCounterInterval);   const fearScenarios = document.querySelectorAll('.fear-scenario'); fearScenarios.forEach(scenario => { scenario.style.borderColor = 'rgba(40, 167, 69, 0.5)'; scenario.style.background = 'linear-gradient(45deg, rgba(40, 167, 69, 0.1), rgba(255, 193, 7, 0.05))'; });}function showSalvationSuccess() {  const salvationOverlay = document.createElement('div'); salvationOverlay.innerHTML = ` <div style=" position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: linear-gradient(45deg, rgba(40, 167, 69, 0.95), rgba(0, 0, 0, 0.9)); display: flex; align-items: center; justify-content: center; z-index: 10000; animation: salvationFadeIn 0.5s ease; "> <div style=" background: linear-gradient(45deg, rgba(40, 167, 69, 0.9), rgba(255, 193, 7, 0.8)); padding: 60px; border-radius: 25px; text-align: center; max-width: 600px; color: white; animation: salvationPop 0.8s ease; box-shadow: 0 30px 80px rgba(40, 167, 69, 0.6); "> <i class="fas fa-shield-alt" style="font-size: 5rem; color: #28a745; margin-bottom: 25px; animation: salvationShield 1s ease-in-out infinite alternate;"></i> <h2 style="margin-bottom: 20px; font-size: 2.5rem;">ЗАЩИТАТА АКТИВИРАНА!</h2> <p style="margin-bottom: 25px; font-size: 1.2rem;">Загубите са спрени! Вашето финансово бъдеще е в сигурност.</p> <div style="background: rgba(255,255,255,0.2); padding: 20px; border-radius: 15px; margin-bottom: 25px;"> <h3 style="color: #28a745; margin-bottom: 15px;">ЩЕ ПОЛУЧИТЕ:</h3> <ul style="list-style: none; padding: 0;"> <li style="margin-bottom: 10px;">✅ Безплатен достъп за 7 дни</li> <li style="margin-bottom: 10px;">✅ Спиране на загубите веднага</li> <li style="margin-bottom: 10px;">✅ Персонален финансов план</li> <li>✅ 100% гаранция за връщане</li> </ul> </div> <button onclick="this.parentElement.parentElement.remove()" style=" background: #28a745; border: none; padding: 20px 40px; border-radius: 15px; color: white; font-weight: 700; font-size: 16px; cursor: pointer; box-shadow: 0 10px 25px rgba(40, 167, 69, 0.4); ">ЗАПОЧНЕТЕ СЕГА</button> </div> </div> `;  document.body.appendChild(salvationOverlay);}const fearStyles = `@keyframes fearCounterShock { 0% { transform: scale(1); } 50% { transform: scale(1.3); color: #ff0000; } 100% { transform: scale(1); }}@keyframes aggressivePulse { 0% { transform: scale(1) rotate(0deg); } 50% { transform: scale(1.2) rotate(-5deg); } 100% { transform: scale(1) rotate(0deg); }}@keyframes salvationActivated { 0% { filter: brightness(1); } 50% { filter: brightness(1.3) hue-rotate(120deg); } 100% { filter: brightness(1); }}@keyframes salvationFadeIn { from { opacity: 0; } to { opacity: 1; }}@keyframes salvationPop { from {  opacity: 0;  transform: scale(0.7) translateY(-50px);  } to {  opacity: 1;  transform: scale(1) translateY(0);  }}@keyframes salvationShield { from {  transform: scale(1); filter: drop-shadow(0 0 20px rgba(40, 167, 69, 0.6)); } to {  transform: scale(1.1); filter: drop-shadow(0 0 40px rgba(40, 167, 69, 0.9)); }}@keyframes barPulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.7; }}`;const fearStyleSheet = document.createElement('style');fearStyleSheet.textContent = fearStyles;document.head.appendChild(fearStyleSheet); function initEnhancedFearFeatures() {  const glassCards = document.querySelectorAll('.fear-glass-card[data-shock="true"]');  glassCards.forEach((card, index) => {  card.addEventListener('mouseenter', () => { card.style.animation = 'cardShock 0.5s ease-in-out'; });  card.addEventListener('mouseleave', () => { card.style.animation = ''; }); });}function scrollToDownload() { const downloadSection = document.getElementById('download'); if (downloadSection) { downloadSection.scrollIntoView({  behavior: 'smooth', block: 'start' }); }}const enhancedAnimationsCSS = `@keyframes cardShock { 0% { transform: scale(1); } 50% { transform: scale(1.05); box-shadow: 0 25px 50px rgba(255, 71, 87, 0.4); } 100% { transform: scale(1); }}@keyframes lossShock { 0% { transform: scale(1); } 50% { transform: scale(1.3); color: #ffd700; } 100% { transform: scale(1); color: #fff; }}@keyframes urgentFlicker { 0%, 100% { opacity: 1; } 50% { opacity: 0.6; color: #ff1a1a; }}@keyframes countUp { 0% { transform: translateY(10px); opacity: 0; } 100% { transform: translateY(0); opacity: 1; }}`;const enhancedStyleSheet = document.createElement('style');enhancedStyleSheet.textContent = enhancedAnimationsCSS;document.head.appendChild(enhancedStyleSheet);function initPhoneFloatingCards() { const floatingCards = document.querySelectorAll('.floating-card');  floatingCards.forEach((card, index) => {  card.addEventListener('mousemove', (e) => { const rect = card.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top;  const glow = card.querySelector('.card-glow'); if (glow) { glow.style.background = `radial-gradient(circle at ${x}px ${y}px, rgba(255,255,255,0.4), transparent 70%)`; } });   card.addEventListener('mousemove', (e) => { const rect = card.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top;  const centerX = rect.width / 2; const centerY = rect.height / 2;  const rotateX = (y - centerY) / 15; const rotateY = (centerX - x) / 15;  card.style.transform = `translateY(-8px) scale(1.05) perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg)`; });  card.addEventListener('mouseleave', () => { card.style.transform = 'translateY(0) perspective(1000px) rotateX(0) rotateY(0)'; }); });   floatingCards.forEach((card, index) => { const randomDelay = Math.random() * 2; const randomDuration = 3 + Math.random() * 2;  card.style.animation = `floatIn 0.6s ease-out ${0.1 * (index + 1)}s forwards,  gentleFloat ${randomDuration}s ease-in-out ${randomDelay}s infinite`; });}const floatAnimationCSS = `@keyframes gentleFloat { 0%, 100% { transform: translateY(0) rotate(0deg); } 25% { transform: translateY(-10px) rotate(1deg); } 75% { transform: translateY(-5px) rotate(-1deg); }}`;const floatStyleSheet = document.createElement('style');floatStyleSheet.textContent = floatAnimationCSS;document.head.appendChild(floatStyleSheet);if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initPhoneFloatingCards);} else { initPhoneFloatingCards();}function initTestimonialsColumns() {  const testimonialsSection = document.querySelector('.testimonials-columns-section'); if (testimonialsSection) { const visibilityObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { const inners = entry.target.querySelectorAll('.testimonials-column-inner'); inners.forEach(inner => { inner.style.animationPlayState = entry.isIntersecting ? 'running' : 'paused'; }); }); }, { threshold: 0.1 });  visibilityObserver.observe(testimonialsSection); }}if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initTestimonialsColumns);} else { initTestimonialsColumns();}function initOrbitalFearSection() { const orbitalItems = document.querySelectorAll('.orbital-item'); const fearCards = document.querySelectorAll('.fear-detail-card'); const fearOverlay = document.querySelector('.fear-overlay'); const closeButtons = document.querySelectorAll('.fear-card-close'); const fearSection = document.querySelector('.fear-orbital-section');  if (!orbitalItems.length) return;  let rotationAngle = 0; let autoRotate = true; let animationId = null; let isVisible = false;   function getRadius() { if (window.innerWidth <= 480) return 140; if (window.innerWidth <= 768) return 200; return 360;  }   function calculateNodePosition(index, total, angle) { const itemAngle = ((index / total) * 360 + angle) % 360; const radius = getRadius(); const radian = (itemAngle * Math.PI) / 180;  const x = radius * Math.cos(radian); const y = radius * Math.sin(radian);   const opacity = Math.max(0.5, Math.min(1, 0.5 + 0.5 * ((1 + Math.sin(radian)) / 2)));  return { x, y, opacity }; }   function updatePositions() { const total = orbitalItems.length;  orbitalItems.forEach((item, index) => { const pos = calculateNodePosition(index, total, rotationAngle); const transform = `translate(${pos.x}px, ${pos.y}px)`;  item.style.transform = transform; item.style.setProperty('--current-transform', transform); item.style.opacity = pos.opacity; }); }   function animate() { if (autoRotate && isVisible) { rotationAngle = (rotationAngle + 0.3) % 360; updatePositions(); } animationId = requestAnimationFrame(animate); }   function startAnimation() { if (!animationId) { animationId = requestAnimationFrame(animate); } }   function stopAnimation() { if (animationId) { cancelAnimationFrame(animationId); animationId = null; } }   orbitalItems.forEach((item, index) => { item.addEventListener('click', (e) => { e.stopPropagation(); const fearId = item.getAttribute('data-fear'); openFearCard(fearId, index); }); });   closeButtons.forEach(btn => { btn.addEventListener('click', (e) => { e.stopPropagation(); closeFearCards(); }); });   if (fearOverlay) { fearOverlay.addEventListener('click', closeFearCards); }   document.addEventListener('keydown', (e) => { if (e.key === 'Escape') { closeFearCards(); } });  function openFearCard(fearId, nodeIndex) {  autoRotate = false;   const total = orbitalItems.length; const targetAngle = 270 - (nodeIndex / total) * 360; rotationAngle = targetAngle; updatePositions();   orbitalItems.forEach(item => item.classList.remove('active'));   const activeItem = document.querySelector(`.orbital-item[data-fear="${fearId}"]`); if (activeItem) { activeItem.classList.add('active'); }   fearCards.forEach(card => card.classList.remove('active'));   const targetCard = document.querySelector(`.fear-detail-card[data-card="${fearId}"]`); if (targetCard) { targetCard.classList.add('active'); }   if (fearOverlay) { fearOverlay.classList.add('active'); }   document.body.style.overflow = 'hidden'; }  function closeFearCards() {  autoRotate = true;   orbitalItems.forEach(item => item.classList.remove('active'));   fearCards.forEach(card => card.classList.remove('active'));   if (fearOverlay) { fearOverlay.classList.remove('active'); }   document.body.style.overflow = ''; }   if (fearSection) { let hasAnimatedIn = false;  const visibilityObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { isVisible = entry.isIntersecting; if (isVisible) { startAnimation();   if (!hasAnimatedIn) { hasAnimatedIn = true; fearSection.classList.add('animate-in'); } } }); }, { threshold: 0.15 });  visibilityObserver.observe(fearSection); }   window.addEventListener('resize', () => { updatePositions(); });   updatePositions(); startAnimation();}if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initOrbitalFearSection);} else { initOrbitalFearSection();}function initHowItWorksAnimations() { const observerOptions = { threshold: 0.1, rootMargin: '0px' };  const stepsObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { const stepCards = entry.target.querySelectorAll('.step-card'); stepCards.forEach((card, index) => { setTimeout(() => { card.classList.add('animate-in'); }, index * 150); });   initProgressTracking(); } }); }, observerOptions);  const howSection = document.querySelector('.how-it-works-section'); if (howSection) { stepsObserver.observe(howSection); }   initCardTiltEffect();   initMouseGlowEffect();}function initProgressTracking() { const progressFill = document.querySelector('.how-progress-fill'); const progressSteps = document.querySelectorAll('.progress-step'); const section = document.querySelector('.how-it-works-section');  if (!progressFill || !progressSteps.length || !section) return;  const updateProgress = () => { const sectionRect = section.getBoundingClientRect(); const sectionTop = sectionRect.top; const sectionHeight = sectionRect.height; const windowHeight = window.innerHeight;    const scrollProgress = Math.max(0, Math.min(1,  (windowHeight - sectionTop) / (sectionHeight + windowHeight * 0.5) ));  progressFill.style.height = `${scrollProgress * 100}%`;   const activeStep = Math.min(4, Math.floor(scrollProgress * 5)); progressSteps.forEach((step, index) => { if (index <= activeStep) { step.classList.add('active'); } else { step.classList.remove('active'); } }); };   const throttledProgress = throttle(updateProgress, 16);  window.addEventListener('scroll', throttledProgress, passiveEvent); updateProgress();}function initCardTiltEffect() { const cards = document.querySelectorAll('.step-card');  cards.forEach(card => { card.addEventListener('mousemove', (e) => { const rect = card.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top;  const centerX = rect.width / 2; const centerY = rect.height / 2;  const rotateX = (y - centerY) / 10; const rotateY = (centerX - x) / 10;  card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateY(-15px) scale(1.02)`; });  card.addEventListener('mouseleave', () => { card.style.transform = ''; }); });}function initMouseGlowEffect() { const cards = document.querySelectorAll('.step-card');  cards.forEach(card => { const glow = card.querySelector('.step-card-glow'); if (!glow) return;  card.addEventListener('mousemove', (e) => { const rect = card.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top;  glow.style.background = `radial-gradient(circle 300px at ${x}px ${y}px, rgba(102, 126, 234, 0.4) 0%, transparent 70%)`; }); });}function initTestimonialsAnimations() { const observerOptions = { threshold: 0.1, rootMargin: '0px' };  const testimonialsObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { const cards = entry.target.querySelectorAll('.testimonial-card'); cards.forEach((card, index) => { setTimeout(() => { card.style.opacity = '1'; card.style.transform = 'translateY(0)'; }, index * 150); }); } }); }, observerOptions);  const testimonialsGrid = document.querySelector('.testimonials-grid'); if (testimonialsGrid) {  const cards = testimonialsGrid.querySelectorAll('.testimonial-card'); cards.forEach(card => { card.style.opacity = '0'; card.style.transform = 'translateY(30px)'; card.style.transition = 'opacity 0.6s ease, transform 0.6s ease'; });  testimonialsObserver.observe(testimonialsGrid); }}document.addEventListener('DOMContentLoaded', function() { initHowItWorksAnimations(); initTestimonialsAnimations(); initFooterAnimations();  console.log('Нови секции заредени успешно! ✅');});function animateStatsCounter(element, target, suffix = '', duration = 2000) { const start = 0; const increment = target / (duration / 16);  let current = start;  const timer = setInterval(() => { current += increment; if (current >= target) { element.textContent = target + suffix; clearInterval(timer); } else { element.textContent = Math.floor(current) + suffix; } }, 16);}const statsSection = document.querySelector('.stats-achievements-section');if (statsSection) { const statsObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) {  entry.target.classList.add('animate-in');   const statItems = entry.target.querySelectorAll('.stat-item');  statItems.forEach((item, index) => { const statNumber = item.querySelector('.stat-number-large'); const target = parseInt(statNumber.getAttribute('data-target'));   let suffix = ''; if (index === 0) suffix = '+';  if (index === 1) suffix = '%';  if (index === 2) suffix = '%';    setTimeout(() => { animateStatsCounter(statNumber, target, suffix); }, 200 + (index * 200)); });   statsObserver.unobserve(entry.target); } }); }, { threshold: 0.3 });  statsObserver.observe(statsSection);}function initFooterAnimations() { const footer = document.querySelector('.footer'); if (!footer) return;  const footerObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) {  const columns = entry.target.querySelectorAll('.footer-column'); columns.forEach((column, index) => { setTimeout(() => { column.style.opacity = '1'; column.style.transform = 'translateY(0)'; }, index * 150); });   const footerBottom = entry.target.querySelector('.footer-bottom'); if (footerBottom) { setTimeout(() => { footerBottom.style.opacity = '1'; footerBottom.style.transform = 'translateY(0)'; }, columns.length * 150 + 200); }   footerObserver.unobserve(entry.target); } }); }, { threshold: 0.2 });   const columns = footer.querySelectorAll('.footer-column'); columns.forEach(column => { column.style.opacity = '0'; column.style.transform = 'translateY(30px)'; column.style.transition = 'opacity 0.6s ease-out, transform 0.6s ease-out'; });  const footerBottom = footer.querySelector('.footer-bottom'); if (footerBottom) { footerBottom.style.opacity = '0'; footerBottom.style.transform = 'translateY(20px)'; footerBottom.style.transition = 'opacity 0.6s ease-out, transform 0.6s ease-out'; }  footerObserver.observe(footer);}function openHelpCenter() { const modal = document.getElementById('helpCenterModal'); if (modal) { modal.classList.add('active'); document.body.style.overflow = 'hidden';   setTimeout(() => { const searchInput = document.getElementById('helpSearch'); if (searchInput) { searchInput.focus(); } }, 300);   if (typeof gtag !== 'undefined') { gtag('event', 'help_center_opened', { 'event_category': 'engagement', 'event_label': 'footer_link' }); } }}function closeHelpCenter() { const modal = document.getElementById('helpCenterModal'); if (modal) { modal.classList.remove('active'); document.body.style.overflow = '';   const searchInput = document.getElementById('helpSearch'); if (searchInput) { searchInput.value = ''; filterHelpContent(''); }   const categories = modal.querySelectorAll('.help-category'); categories.forEach(category => { category.classList.remove('active'); }); }}function toggleHelpCategory(categoryElement) { categoryElement.classList.toggle('active');   const allCategories = document.querySelectorAll('.help-category'); allCategories.forEach(cat => { if (cat !== categoryElement && cat.classList.contains('active')) { cat.classList.remove('active'); } });}function initHelpCenterSearch() { const searchInput = document.getElementById('helpSearch'); if (searchInput) { searchInput.addEventListener('input', function(e) { filterHelpContent(e.target.value); });   searchInput.addEventListener('keydown', function(e) { if (e.key === 'Escape') { closeHelpCenter(); } }); }}function filterHelpContent(searchTerm) { const searchLower = searchTerm.toLowerCase().trim(); const categories = document.querySelectorAll('.help-category'); const helpItems = document.querySelectorAll('.help-item');  if (!searchTerm) {  categories.forEach(cat => { cat.style.display = ''; }); helpItems.forEach(item => { item.style.display = ''; }); return; }  let hasResults = false;  categories.forEach(category => { const categoryTitle = category.querySelector('h3').textContent.toLowerCase(); const items = category.querySelectorAll('.help-item'); let categoryHasMatch = false;  items.forEach(item => { const itemTitle = item.querySelector('h4').textContent.toLowerCase(); const itemContent = item.querySelector('p').textContent.toLowerCase();  if (itemTitle.includes(searchLower) || itemContent.includes(searchLower)) { item.style.display = ''; categoryHasMatch = true; hasResults = true; } else { item.style.display = 'none'; } });   if (categoryTitle.includes(searchLower) || categoryHasMatch) { category.style.display = ''; if (categoryHasMatch) { category.classList.add('active'); } } else { category.style.display = 'none'; } });   let noResultsMsg = document.querySelector('.help-no-results'); if (!hasResults && searchTerm) { if (!noResultsMsg) { noResultsMsg = document.createElement('div'); noResultsMsg.className = 'help-no-results'; noResultsMsg.innerHTML = ` <div style="text-align: center; padding: 40px 20px;"> <i class="fas fa-search" style="font-size: 48px; color: rgba(44, 44, 44, 0.3); margin-bottom: 16px;"></i> <h3 style="color: rgba(44, 44, 44, 0.7); margin: 0 0 8px 0;">Няма намерени резултати</h3> <p style="color: rgba(44, 44, 44, 0.5); margin: 0;">Опитайте с различни ключови думи или се свържете с нас</p> </div> `; const helpContent = document.querySelector('.help-center-content'); if (helpContent) { helpContent.appendChild(noResultsMsg); } } noResultsMsg.style.display = 'block'; } else if (noResultsMsg) { noResultsMsg.style.display = 'none'; }}document.addEventListener('keydown', function(e) { if (e.key === 'Escape') { const modal = document.getElementById('helpCenterModal'); if (modal && modal.classList.contains('active')) { closeHelpCenter(); } }});function openContactModal(preselectedSubject = null) { const modal = document.getElementById('contactModal'); if (modal) { modal.classList.add('active'); document.body.style.overflow = 'hidden';   if (preselectedSubject) { const subjectSelect = document.getElementById('contactSubject'); if (subjectSelect) { subjectSelect.value = preselectedSubject; } }   setTimeout(() => { const firstInput = document.getElementById('contactName'); if (firstInput) { firstInput.focus(); } }, 300);   if (typeof gtag !== 'undefined') { gtag('event', 'contact_modal_opened', { 'event_category': 'engagement', 'event_label': preselectedSubject || 'contact_form' }); } }}function openFeedbackModal() { openContactModal('feedback');}function closeContactModal() { const modal = document.getElementById('contactModal'); if (modal) { modal.classList.remove('active'); document.body.style.overflow = '';   const form = document.getElementById('contactForm'); if (form) { form.reset();  const errorMessages = form.querySelectorAll('.error-message'); errorMessages.forEach(msg => msg.remove()); const errorInputs = form.querySelectorAll('.error'); errorInputs.forEach(input => input.classList.remove('error')); } }}function submitContactForm(event) { event.preventDefault();  const form = event.target; const submitBtn = form.querySelector('.contact-submit-btn'); const originalText = submitBtn.innerHTML;   const formData = { name: document.getElementById('contactName').value.trim(), email: document.getElementById('contactEmail').value.trim(), subject: document.getElementById('contactSubject').value, message: document.getElementById('contactMessage').value.trim() };   if (!formData.name || !formData.email || !formData.subject || !formData.message) { showFormError('Моля, попълнете всички задължителни полета.'); return; }   const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/; if (!emailRegex.test(formData.email)) { showFormError('Моля, въведете валиден email адрес.'); return; }   submitBtn.disabled = true; submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Изпращане...';   setTimeout(() => {  showFormSuccess('Съобщението ви е изпратено успешно! Ще се свържем с вас скоро.');   form.reset();   setTimeout(() => { closeContactModal(); }, 2000);   if (typeof gtag !== 'undefined') { gtag('event', 'contact_form_submitted', { 'event_category': 'conversion', 'event_label': formData.subject }); } }, 1500);}function showFormError(message) {  const existingErrors = document.querySelectorAll('.form-error-message'); existingErrors.forEach(err => err.remove());   const errorDiv = document.createElement('div'); errorDiv.className = 'form-error-message'; errorDiv.style.cssText = ` background: rgba(255, 107, 107, 0.1); border: 2px solid rgba(255, 107, 107, 0.3); color: #ff6b6b; padding: 12px 16px; border-radius: 12px; margin-bottom: 20px; font-size: 14px; font-weight: 500; display: flex; align-items: center; gap: 10px; `; errorDiv.innerHTML = `<i class="fas fa-exclamation-circle"></i> ${message}`;  const form = document.getElementById('contactForm'); form.insertBefore(errorDiv, form.firstChild);   errorDiv.scrollIntoView({ behavior: 'smooth', block: 'nearest' });}function showFormSuccess(message) {  const existingMessages = document.querySelectorAll('.form-success-message, .form-error-message'); existingMessages.forEach(msg => msg.remove());   const successDiv = document.createElement('div'); successDiv.className = 'form-success-message'; successDiv.style.cssText = ` background: rgba(76, 175, 80, 0.1); border: 2px solid rgba(76, 175, 80, 0.3); color: #4CAF50; padding: 12px 16px; border-radius: 12px; margin-bottom: 20px; font-size: 14px; font-weight: 500; display: flex; align-items: center; gap: 10px; `; successDiv.innerHTML = `<i class="fas fa-check-circle"></i> ${message}`;  const form = document.getElementById('contactForm'); form.insertBefore(successDiv, form.firstChild);   successDiv.scrollIntoView({ behavior: 'smooth', block: 'nearest' });}document.addEventListener('keydown', function(e) { if (e.key === 'Escape') { const contactModal = document.getElementById('contactModal'); const helpModal = document.getElementById('helpCenterModal'); const termsModal = document.getElementById('termsModal'); const privacyModal = document.getElementById('privacyModal');  if (contactModal && contactModal.classList.contains('active')) { closeContactModal(); } else if (helpModal && helpModal.classList.contains('active')) { closeHelpCenter(); } else if (termsModal && termsModal.classList.contains('active')) { closeTermsModal(); } else if (privacyModal && privacyModal.classList.contains('active')) { closePrivacyModal(); } }});document.addEventListener('DOMContentLoaded', function() { initHelpCenterSearch();});function openTermsModal() { const modal = document.getElementById('termsModal'); if (modal) { modal.classList.add('active'); document.body.style.overflow = 'hidden';   if (typeof gtag !== 'undefined') { gtag('event', 'terms_modal_opened', { 'event_category': 'engagement', 'event_label': 'terms_of_service' }); } }}function closeTermsModal() { const modal = document.getElementById('termsModal'); if (modal) { modal.classList.remove('active'); document.body.style.overflow = ''; }}function openPrivacyModal() { const modal = document.getElementById('privacyModal'); if (modal) { modal.classList.add('active'); document.body.style.overflow = 'hidden';   if (typeof gtag !== 'undefined') { gtag('event', 'privacy_modal_opened', { 'event_category': 'engagement', 'event_label': 'privacy_policy' }); } }}function closePrivacyModal() { const modal = document.getElementById('privacyModal'); if (modal) { modal.classList.remove('active'); document.body.style.overflow = ''; }}function initFeaturesCarousel() { const wrapper = document.querySelector('.features-carousel-wrapper'); const cards = document.querySelectorAll('.features-carousel-wrapper .bento-card'); const indicators = document.querySelectorAll('.carousel-indicator'); const swipeHint = document.querySelector('.swipe-hint');  if (!wrapper || !cards.length) return;  let currentIndex = 0; let startX = 0; let currentX = 0; let isDragging = false; let startTime = 0;   function getCardWidth() { const card = cards[0]; const cardWidth = card.offsetWidth; const gap = 20;  return cardWidth + gap; }   function updateCarousel(animate = true) { const cardWidth = getCardWidth(); const offset = -currentIndex * cardWidth;  if (animate) { wrapper.style.transition = 'transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94)'; } else { wrapper.style.transition = 'none'; }  wrapper.style.transform = `translateX(${offset}px)`;   cards.forEach((card, index) => { if (index === currentIndex) { card.classList.add('active'); } else { card.classList.remove('active'); } });   indicators.forEach((indicator, index) => { if (index === currentIndex) { indicator.classList.add('active'); } else { indicator.classList.remove('active'); } });   if (swipeHint && currentIndex > 0) { swipeHint.style.opacity = '0'; setTimeout(() => { swipeHint.style.display = 'none'; }, 300); } }   function goToSlide(index) { currentIndex = Math.max(0, Math.min(index, cards.length - 1)); updateCarousel(true); }   function handleStart(e) { isDragging = true; startTime = Date.now(); startX = e.type.includes('mouse') ? e.pageX : e.touches[0].pageX; currentX = startX; wrapper.classList.add('dragging'); wrapper.style.transition = 'none'; }   function handleMove(e) { if (!isDragging) return;  e.preventDefault(); currentX = e.type.includes('mouse') ? e.pageX : e.touches[0].pageX; const diff = currentX - startX; const cardWidth = getCardWidth(); const offset = -currentIndex * cardWidth + diff;  wrapper.style.transform = `translateX(${offset}px)`; }   function handleEnd(e) { if (!isDragging) return;  isDragging = false; wrapper.classList.remove('dragging');  const diff = currentX - startX; const cardWidth = getCardWidth(); const threshold = cardWidth * 0.2;  const velocity = Math.abs(diff) / (Date.now() - startTime);   if (Math.abs(diff) > threshold || velocity > 0.5) { if (diff > 0 && currentIndex > 0) {  currentIndex--; } else if (diff < 0 && currentIndex < cards.length - 1) {  currentIndex++; } }  updateCarousel(true); }   wrapper.addEventListener('mousedown', handleStart); document.addEventListener('mousemove', handleMove); document.addEventListener('mouseup', handleEnd);   wrapper.addEventListener('touchstart', handleStart, { passive: true }); wrapper.addEventListener('touchmove', handleMove, { passive: false }); wrapper.addEventListener('touchend', handleEnd);   indicators.forEach((indicator, index) => { indicator.addEventListener('click', () => { goToSlide(index); }); });   document.addEventListener('keydown', (e) => { if (!wrapper.closest('.phone-section')) return;  const section = wrapper.closest('.phone-section'); const rect = section.getBoundingClientRect();   if (rect.top < window.innerHeight && rect.bottom > 0) { if (e.key === 'ArrowLeft') { goToSlide(currentIndex - 1); } else if (e.key === 'ArrowRight') { goToSlide(currentIndex + 1); } } });   let resizeTimeout; window.addEventListener('resize', () => { clearTimeout(resizeTimeout); resizeTimeout = setTimeout(() => { updateCarousel(false); }, 100); });   updateCarousel(false);   if (swipeHint) { setTimeout(() => { swipeHint.style.opacity = '0'; setTimeout(() => { swipeHint.style.display = 'none'; }, 300); }, 5000); }}if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initFeaturesCarousel);} else { initFeaturesCarousel();} /** * Tubelight Navigation System * - Active tab tracking based on scroll position * - Smooth scroll to sections * - Animated lamp effect * - Keyboard navigation support */let tubelightNav = { navItems: null, sections: null, currentActiveIndex: 0, isScrolling: false, scrollTimeout: null,   init: function() { this.navItems = document.querySelectorAll('.tubelight-item'); this.sections = document.querySelectorAll('[data-scroll-section]');  if (!this.navItems.length || !this.sections.length) { console.warn('Tubelight nav: Missing navigation items or sections'); return; }   this.setupClickHandlers();   this.setupScrollSpy();   this.setupKeyboardNav();  console.log('✅ Tubelight Navigation initialized'); },   setupClickHandlers: function() { this.navItems.forEach((item, index) => { item.addEventListener('click', (e) => { e.preventDefault();  const sectionId = item.dataset.section; this.scrollToSection(sectionId, index); }); }); },   scrollToSection: function(sectionId, index) {  let targetSection = null;   targetSection = document.getElementById(sectionId);   if (!targetSection) { targetSection = document.querySelector(`.${sectionId}`); }   if (sectionId === 'hero') { targetSection = document.querySelector('.hero'); }  if (!targetSection) { console.warn(`Section not found: ${sectionId}`); return; }   this.setActiveTab(index);   if (window.locomotiveScroll) { this.isScrolling = true; window.locomotiveScroll.scrollTo(targetSection, { duration: 1200, easing: [0.25, 0.0, 0.35, 1.0], disableLerp: false, callback: () => { this.isScrolling = false; } }); } else {  this.isScrolling = true; targetSection.scrollIntoView({  behavior: 'smooth', block: 'start' });  setTimeout(() => { this.isScrolling = false; }, 1200); } },   setActiveTab: function(index) { if (index === this.currentActiveIndex) return;  this.navItems.forEach((item, i) => { if (i === index) { item.classList.add('active'); item.setAttribute('aria-current', 'page'); } else { item.classList.remove('active'); item.removeAttribute('aria-current'); } });  this.currentActiveIndex = index; },   setupScrollSpy: function() {  const sectionMap = new Map();  this.navItems.forEach((item, index) => { const sectionId = item.dataset.section; sectionMap.set(sectionId, index); });   const options = { root: null, rootMargin: '-20% 0px -60% 0px',  threshold: [0, 0.1, 0.5, 0.9, 1] };   const visibleSections = new Map();  const observer = new IntersectionObserver((entries) => {  if (this.isScrolling) return;  entries.forEach(entry => { const section = entry.target; const sectionId = this.getSectionId(section);  if (entry.isIntersecting) { visibleSections.set(sectionId, { ratio: entry.intersectionRatio, top: entry.boundingClientRect.top }); } else { visibleSections.delete(sectionId); } });   let mostVisibleSection = null; let highestRatio = 0; let topMostPosition = Infinity;  visibleSections.forEach((data, sectionId) => { if (data.ratio > highestRatio ||  (data.ratio === highestRatio && data.top < topMostPosition)) { highestRatio = data.ratio; topMostPosition = data.top; mostVisibleSection = sectionId; } });   if (mostVisibleSection && sectionMap.has(mostVisibleSection)) { const index = sectionMap.get(mostVisibleSection); this.setActiveTab(index); } }, options);   this.sections.forEach(section => { observer.observe(section); }); },   getSectionId: function(section) {  if (section.id) return section.id;   if (section.dataset.section) return section.dataset.section;   if (section.classList.contains('hero')) return 'hero';   const classList = Array.from(section.classList); for (let className of classList) { if (className.includes('features')) return 'features'; if (className.includes('pricing')) return 'pricing'; if (className.includes('download')) return 'download'; }  return null; },   setupKeyboardNav: function() { document.addEventListener('keydown', (e) => {  const nav = document.querySelector('.tubelight-nav'); if (!nav || window.getComputedStyle(nav).display === 'none') { return; }   if (e.key === 'ArrowUp' || e.key === 'ArrowDown') { e.preventDefault();  let newIndex = this.currentActiveIndex;  if (e.key === 'ArrowUp') { newIndex = Math.max(0, this.currentActiveIndex - 1); } else { newIndex = Math.min(this.navItems.length - 1, this.currentActiveIndex + 1); }  if (newIndex !== this.currentActiveIndex) { const targetItem = this.navItems[newIndex]; const sectionId = targetItem.dataset.section; this.scrollToSection(sectionId, newIndex); } } }); }};function initTubelightNavigation() {  setTimeout(() => { tubelightNav.init(); }, 500);}if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initTubelightNavigation);} else { initTubelightNavigation();}function initHeroDesktopNav() { const navItems = document.querySelectorAll('.nav-pill-item'); const lamp = document.querySelector('.nav-pill-lamp');  if (!navItems.length || !lamp) return;   navItems.forEach((item, index) => { item.addEventListener('click', function(e) { e.preventDefault();   navItems.forEach(navItem => navItem.classList.remove('active'));   this.classList.add('active');   const targetId = this.getAttribute('href'); const targetSection = document.querySelector(targetId);  if (targetSection && locomotiveScroll) { locomotiveScroll.scrollTo(targetSection, { duration: 1000, easing: [0.16, 1, 0.3, 1], offset: 0 }); } else if (targetSection) { targetSection.scrollIntoView({ behavior: 'smooth', block: 'start' }); } }); });  console.log('🧭 Hero Desktop Navigation initialized (Static)!');}if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initHeroDesktopNav);} else { initHeroDesktopNav();}